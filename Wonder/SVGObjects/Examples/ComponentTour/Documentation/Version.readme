SVGComponentTour1B:

1. Removing isStateless method from SVG pages as they aren't components,
they result in directAction pages from not deallocating. (i.e memory leak)

SVGComponentTour2A:

1. Converted to pure Java usingJavaConverter.
2. Imported project.
3. Re-writing non converted WebScript files.
4. Re organising Web Components into HTML and SVG groups

SVGComponentTour2B:

1. Re-synching project with ProjectBuilderWO
2. Build and run on MacOS X.1/WO5u3 with SVG plugin X 3.0