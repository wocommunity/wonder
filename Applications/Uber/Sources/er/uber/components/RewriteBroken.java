package er.uber.components;

import com.webobjects.appserver.WOContext;

public class RewriteBroken extends UberComponent {
    public RewriteBroken(WOContext context) {
        super(context);
    }
}