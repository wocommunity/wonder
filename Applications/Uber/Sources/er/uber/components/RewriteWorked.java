package er.uber.components;

import com.webobjects.appserver.WOContext;

public class RewriteWorked extends UberComponent {
    public RewriteWorked(WOContext context) {
        super(context);
    }
}