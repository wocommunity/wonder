<project name="JavaWOExtensions" default="all" basedir="..">
    <property name="project.name" value="${ant.project.name}" />

    <!-- dont touch from here on 
    <property name="project.dir" value="${basedir}" /> 
    <property name="wonder.root" value="${project.dir}" />-->

    <target name="clean">
        <ant antfile="../../../Build/build/common.xml" target="${ant.project.name}.clean" />
    </target>

    <target name="install">
        <ant antfile="../../../Build/build/common.xml" target="install" />
    </target>

    <target name="usage">
        <ant antfile="../../../Build/build/common.xml" target="usage" />
    </target>

    <target name="all">
        <ant antfile="../../../Build/build/common.xml" target="${ant.project.name}.all" />
    </target>
</project>
