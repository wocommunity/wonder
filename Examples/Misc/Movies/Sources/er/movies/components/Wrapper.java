package er.movies.components;

import com.webobjects.appserver.WOContext;

import er.extensions.components.ERXComponent;

public class Wrapper extends ERXComponent {
    public Wrapper(WOContext context) {
        super(context);
    }
}