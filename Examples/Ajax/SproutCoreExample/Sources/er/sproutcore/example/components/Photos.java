package er.sproutcore.example.components;

import com.webobjects.appserver.WOComponent;
import com.webobjects.appserver.WOContext;

public class Photos extends WOComponent {
    public Photos(WOContext context) {
        super(context);
    }
}