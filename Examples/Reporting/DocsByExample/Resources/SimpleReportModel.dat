{
    AttributeDef = (
        {keyPath = revenue; label = Revenue; total = YES; }, 
        {keyPath = title; label = Title; }, 
        {keyPath = dateReleased; label = "Date Released"; }
    ); 
    GroupDef = (
        {
            subCriteriaList = (
                {
                    groupEdges = true; 
                    key = category; 
                    possibleValues = (Action, Drama, Cult); 
                    possibleValuesUseType = usePredefined; 
                    useMethod = NO; 
                    useTimeFormat = NO; 
                }
            ); 
            userInfo = {
                IS_STRING = YES; 
                OTHER_LABEL = Other; 
                SHOW_OTHER = YES; 
                SHOW_TOTAL = YES; 
                TOTAL_LABEL = Total; 
                calendarFormat = "%m/%d/%Y"; 
                compoundSeparator = "|"; 
                rangeSeparator = " to "; 
            }; 
        }
    ); 
}